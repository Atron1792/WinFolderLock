<Window x:Class="WinFolderLock.PasswordInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinFolderLock"
        mc:Ignorable="d"
        Title="Password Input" Height="175" Width="300" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Icon="/Resources/Locked.ico" Topmost="True" MinHeight="175" MinWidth="300"
        FocusManager.FocusedElement="{Binding ElementName=UserPasswordBox}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="InstructionText" Text="Please enter your password:" Grid.Row="0" Margin="0,0,0,10" FontSize="14"/>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <PasswordBox x:Name="UserPasswordBox" Grid.Column="0" Height="25" VerticalContentAlignment="Center"/>
            <TextBox x:Name="UserTextBox" Grid.Column="0" Height="25" VerticalContentAlignment="Center" Visibility="Collapsed"/>

            <Button x:Name="ShowPasswordButton" Grid.Column="1" Height="25" Margin="5,0,0,0" Content="👁" FontSize="16"
                    PreviewMouseDown="ShowPasswordButton_PreviewMouseDown" 
                    PreviewMouseUp="ShowPasswordButton_PreviewMouseUp" 
                    MouseLeave="ShowPasswordButton_MouseLeave" HorizontalAlignment="Left" Width="26"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="69,0,0,0">
            <Button x:Name="OkButton" Content="OK" Width="75" Margin="0,0,10,0" Click="OkButton_Click" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="75" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
